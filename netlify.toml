[build]
  command = "bun run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — يضمن عمل التنقل بين الصفحات بعد التحديث
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
